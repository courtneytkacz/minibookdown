# Adding Content

To add chapters to your textbook, simply create new files for each one. Bookdown supports Markdown formatting. You can also insert graphics, including images, charts, and figures.

Here is an example:

par(mar = c(4, 4, 0.1, 0.1))
plot(pressure, pch = 19, type = "b")
